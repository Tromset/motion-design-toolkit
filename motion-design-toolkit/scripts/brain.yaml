# brain.yaml — folder variables (brAIn rule 6)
name: scripts
purpose: "Thin wrapper that runs the repo-root plugin installer."
parent: ../brain.yaml
children: []
files:
  README.md: "Install-script hub."
  install-local.sh: "exec ../scripts/install-local.sh — symlink the git root as the plugin."
when_to_read:
  "How do I install Pro Design on a machine?": install-local.sh
  "Where did the Origin/clone installer go?": README.md
  "How do I install design skills only?": README.md
links: {}
